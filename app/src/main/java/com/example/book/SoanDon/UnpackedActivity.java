package com.example.book.SoanDon;import android.os.Bundle;import android.widget.ArrayAdapter;import android.widget.ListView;import androidx.appcompat.app.AppCompatActivity;import com.example.book.R;public class UnpackedActivity extends AppCompatActivity {    String list[] = {"Đơn hàng 1","Đơn hàng 2","ĐƠn hàng 3"};    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.packager_unpacked_layout);        ListView listView = findViewById(R.id.lstView);        setTitle("Chưa đóng gói");        ArrayAdapter<String> adapter = new ArrayAdapter<String>(this,R.layout.packager_unpacked_items,R.id.txtDonhang,list);        listView.setAdapter(adapter);    }}