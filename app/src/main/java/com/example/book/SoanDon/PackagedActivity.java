package com.example.book.SoanDon;import android.os.Bundle;import android.widget.ArrayAdapter;import android.widget.ListView;import androidx.appcompat.app.AppCompatActivity;import com.example.book.R;public class PackagedActivity extends AppCompatActivity {    String list[] = {"Đơn hàng 1","Đơn hàng 2","ĐƠn hàng 3"};    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.packager_packaged_layout);        ListView listView = findViewById(R.id.listPackaged);        setTitle("Đã đóng gói");        ArrayAdapter<String> adapter = new ArrayAdapter<String>(this,R.layout.packager_packaged_items,R.id.txtPackaged,list);        listView.setAdapter(adapter);    }}