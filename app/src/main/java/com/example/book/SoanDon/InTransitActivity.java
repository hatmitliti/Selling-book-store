package com.example.book.SoanDon;import android.os.Bundle;import android.widget.ArrayAdapter;import android.widget.ListView;import androidx.appcompat.app.AppCompatActivity;import com.example.book.R;public class InTransitActivity extends AppCompatActivity {    String list[] = {"Đơn hàng 1","Đơn hàng 2","ĐƠn hàng 3"};    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.packager_in_transit_layout);        ListView listView = findViewById(R.id.listIntransit);        setTitle("Đang vận chuyển");        ArrayAdapter<String> adapter = new ArrayAdapter<String>(this,R.layout.packager_in_transit_items,R.id.txIntransit,list);        listView.setAdapter(adapter);    }}